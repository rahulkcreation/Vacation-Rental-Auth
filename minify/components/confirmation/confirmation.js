(function(window){'use strict';window.authmeConfirm=function(title,message,btnText){return new Promise(function(resolve){var modal=document.getElementById('authme-confirm-modal');var titleEl=document.getElementById('authme-confirm-title');var messageEl=document.getElementById('authme-confirm-message');var proceedBtn=document.getElementById('authme-confirm-proceed');var cancelBtn=document.getElementById('authme-confirm-cancel');if(!modal||!titleEl||!messageEl||!proceedBtn||!cancelBtn){console.error('AuthMe Confirm: Modal elements not found.');resolve(false);return;}titleEl.textContent=title||'Confirm Action';messageEl.textContent=message||'Are you sure you want to proceed?';proceedBtn.textContent=btnText||'Confirm';modal.style.display='flex';modal.classList.remove('hiding');var onProceed=function(){cleanup();resolve(true);};var onCancel=function(){cleanup();resolve(false);};var cleanup=function(){modal.classList.add('hiding');setTimeout(function(){modal.style.display='none';},300);proceedBtn.removeEventListener('click',onProceed);cancelBtn.removeEventListener('click',onCancel);};proceedBtn.addEventListener('click',onProceed);cancelBtn.addEventListener('click',onCancel);});};})(window);